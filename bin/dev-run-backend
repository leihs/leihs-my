#!/bin/bash -eu

set -euo
PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"
cd $PROJECT_DIR
source $PROJECT_DIR/shared-clj/clojure/bin/activate
clj -M -m leihs.my.main --dev-mode true --repl true run "$@"

# vi: ft=sh
